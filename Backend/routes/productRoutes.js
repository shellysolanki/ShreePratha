// import express from 'express';
// import { uploadMakeup, getAllMakeup, getMakeupById } from '../controllers/makeupController.js';
// import { verifyToken } from '../middleware/auth.js';

// const router = express.Router();

// // 🔐 Admin-only makeup upload
// router.post('/upload', verifyToken, uploadMakeup);

// // 🛒 Public routes
// router.get('/', getAllMakeup);
// router.get('/:id', getMakeupById);

// export default router;